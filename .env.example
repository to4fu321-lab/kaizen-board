# AI文章サポート（箇条書き → 報告文）に使う Anthropic の APIキー。
# 未設定でもアプリは動く（「整える」ボタンがエラーを返すだけ）。
#
# ローカルで試すときは .env.local にコピーして値を入れる（.env* は git 管理外）。
# 本番は Vercel の Settings > Environment Variables に登録する。
# ブラウザには渡らないので NEXT_PUBLIC_ は絶対に付けないこと。
ANTHROPIC_API_KEY=
